// import { action } from '@storybook/addon-actions';
import Info from './Info.svelte';

export default {
  title: 'Info',
  component: Info,
};

export const Text = () => ({
  Component: Info,
  props: {},
});
