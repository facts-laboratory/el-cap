import type { Meta } from '@storybook/react';
import TopCoinsCard from './top-coins-card';

const Story: Meta<typeof TopCoinsCard> = {
  component: TopCoinsCard,
  title: 'TopCoinsCard',
};
export default Story;

export const Primary = {
  args: {},
};
